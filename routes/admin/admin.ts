// post
  // 1 user({aircraftid, email, role})
    // ensure request.aircraft.user.role >= this.role

// get
  // n users({aircraftid}) 

// put
  // 1 user({aircraftid, email, role, roleid})
    // ensure request.aircraft.user.role >= this.role

// delete
  // 1 user({roleid})
